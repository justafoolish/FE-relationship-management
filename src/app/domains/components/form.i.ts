export enum FORM_CONTROLS {
  TEXT = 'text',
  MAIL = 'email',
  PASSWORD = 'password',
  TEL = 'tel',
  // NUMBER = 'number',
  // OTP = 'OTP',
  // SELECT = 'select',
  // CHECKBOX = 'checkbox',
  // SLIDER = 'slider',
  // TEXT_AREA = 'text-area',
  // DATE_PICKER = 'date',
  // TIME_PICKER = 'time',
}

export enum ERROR_MODES {
  ENABLE = 'ENABLE',
  DISABLE = 'DISABLE',
  FLEX = 'FLEX',
}
