export enum FORM_CONTROLS {
  TEXT = 'text',
  MAIL = 'email',
  PASSWORD = 'password',
  TEL = 'tel',
  DATE_PICKER = 'date',
  // NUMBER = 'number',
  // OTP = 'OTP',
  // SELECT = 'select',
  // CHECKBOX = 'checkbox',
  // SLIDER = 'slider',
  // TEXT_AREA = 'text-area',
  // TIME_PICKER = 'time',
}

export enum ERROR_MODES {
  ENABLE = 'ENABLE',
  DISABLE = 'DISABLE',
  FLEX = 'FLEX',
}
